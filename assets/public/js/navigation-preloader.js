(function () {
    'use strict';

    /*
     * Navigation preloader intentionally disabled.
     * Single-post pages should render immediately, while only the live map
     * keeps its own delayed loading indicator.
     */
}());
